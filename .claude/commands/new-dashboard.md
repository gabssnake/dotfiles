---
description: Create a vanilla HTML/CSS/JS static page
---

Create a static HTML page with vanilla HTML, CSS, and JavaScript.

$ARGUMENTS

Keep the code simple and minimalistic. Avoid external dependencies and compilation steps. Use modern browser APIs. Libraries via CDN only (e.g., Chart.js for charts), no build step.
